#!/bin/sh
# Q4OS post-uninstall root hook for Classic-X
set -e

# Clean up APT repository entry on uninstall
if [ -f /etc/apt/sources.list.d/classicx.list ]; then
    rm -f /etc/apt/sources.list.d/classicx.list
fi

exit 0
